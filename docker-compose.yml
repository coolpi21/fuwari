version: '3'

services:
  app:
    image: fuwari:latest
    build: .
    container_name: fuwari
    ports:
      - "17000:9999"
    restart: unless-stopped
    volumes:
      # 可选：如果你需要持久化存储
      # - ./data:/app/data 